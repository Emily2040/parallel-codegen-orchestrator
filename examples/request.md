# Example Request

Create a FastAPI notes service with token auth, SQLite persistence, and pytest coverage.

Constraints:
- Use Python 3.12
- Organize the work into resumable modules
- Use parallel execution only where module boundaries are genuinely independent
- Provide a final handoff report with exact verification commands
